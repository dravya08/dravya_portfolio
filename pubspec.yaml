name: dravya_portfolio           # ← this was cut off in your paste
description: Dravya Gohil - Flutter Developer Portfolio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  url_launcher: ^6.2.4
  flutter_animate: ^4.5.0
  visibility_detector: ^0.4.0+2   # removed scrollable_positioned_list (unused)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets section removed (no assets/ folder = Flutter warning)